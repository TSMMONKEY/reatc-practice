import React from 'react';

const Functional = ({children}) => {
    return (
        <div>
            <h1>Functional Component</h1>
            {children}
        </div>
    );
}

export default Functional;
